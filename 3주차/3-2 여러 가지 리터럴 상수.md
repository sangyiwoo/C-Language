## Ex03_02
```c
#include <stdio.h>

int main(void)
{
    char grade = '\x41'; //문자형 변수 아스키코드(\x41은 아스키 코드가 0x41인 'A'문자)로 저장해 선언(리터럴 상수로 초기화)
    int data = 0x7b; //정수형 변수 0x7b(0x7b는 16 진수로 10진수로는 123, 8진수는 173임)로 저장해 선언
    unsigned int age = 75U; //
    ```